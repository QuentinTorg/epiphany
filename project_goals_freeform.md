I've been working for some time trying to develop a agent assisted notes taking workflow. The goal is to get assistance from an agent actively turning my disjoint note-taking thoughts into a useful summary, which then can be stored for long-term recall.

I had made a first pass, but I think the first pass kind of got out from under me. And at this point I'd like to do a better plan for a second pass. I think the first step is to write a detailed product spec that clearly outlines what is important to me in a final product. After that, it will be important to create an implementation doc that details how things should be implemented. By this I don't mean a step-by-step implementation plan, but instead an overall view of the implementation details required to meet the product spec.

at this point I'm thinking the entire note-taking pipeline shoudl be turned into a skill that can be installed using different agents installation pipeline such as Gemini extensions or claude code plugins. I think skills are naturally set up to teach agents to automatically use tools and follow workflows such as note taking. I think they also still leave things flexible for the agent to call an audible if the user asks for more.  I think some of the structure of how I expect the user to call commands maybe isn't the best. It could be better to use actual built-in slash commands which are effectively canned prompts instead of requiring the user remember how to use these colon based commands.

## High level design goals:
I sort of mix implementation with the design goals here, maybe you can help clarify.

I want to be able to type notes to an agent, and the agent stores the raw snippets of information for later in a raw thread. the content of the thread can be converted into a comprehensive human readable summary of the thread.

I also want the note information to be in a useful format so that agents can search for information within the docuemntation later. Information from threads should be distilled into other topics that can be updated from any new thread. the topics will be living documents that are summaries that contain the useful current information about each topic, (along with some sort of information about when the information was added maybe?).
I'm imagining these topics can then also coalesce into broader and broader topic groups that can be used for an agent to query using a progressive disclosure technique where it can start at the broadest level and work out to more detailed documents until it finds the information that was queried by the user.

This system of ingesting new raw information and then storing it in summaries will allow agents to quickly search through the docs and provide useful insight to general questions that they may be asked. Imagine questions like "what was billy working on last week" or "did we decide to use the 12V or 24V bus for the computer?" or "last time I was debugging this connectivity issue on the robot, what was the issue?" or "who did I meet with last wednesday" or "who was in that meeting" or "what was the test plan last time we were at this test site?"

In a perfect world, I will also have mechanisms for ingesting other documents to help me build a general knowledge base. Imagine adding a customer requirements doc to the database. the requirements doc could be stored as the entire doc, but the contents could still be summarized and distilled out into topics.

I want to use this into a superhuman memory that I can use for higher level decision making. I care a lot about storing in a format that makes it most useful to an agent, where the agent isn't flooded with information when trying to make a query because docs are too broad, but also there is still a mechanism for drilling down to get the detail they will need for any query. I understand agents have large contexts, but it would be very nice if smaller agents could use this tool effectively as well.

I'm imagining there eventually will be a single memory file entrypoint that explains in general the contents of the entire database, and links between all the items. All of these memory docs will be updated anytime a note is updated. For efficiency, maybe it makes sense for the memory updates to only happen once at the end of the note, but the summary updates should happen as we go so the agent can effectively participate in things like open questions. maybe in order to be a good participant the agent will still need to actively query the note databases to keep track of things. if it sees me tracking something that has already beent racked, it can indicate to me that maybe these details don't need to be duplicated, and remind me of what was said last time live while the note is being typed

A perfect agent would behvave like a note taking assistant, not just an input mechanism. The agent should be an active participant in the note taking process, keeping track of open questions, and asking for clarifications when not enough detail was provided, letting the user know when it needs more information to file the information correctly so it stays useful later.

I also want this to be easy to deploy. I imagine using agent skills is the best mechanism because many agents support them. it also allows us to use different workspaces

## considerations
I want live updates from agent. Open questions, offers to update memory files, feedback if topics have already been covered, or if new note is a contradition to the old note. Agent should have the directive to be as helpful as possible and trying to stay ahead of me and make sure I don't make mistakes

threads/live note snippets should be tracked in as raw of a form as possible and become immutable archive. metadata for tracking date change, and conversation thread information should be tracked at a miimum, maybe other information too.

memories/summaries should be updated. the content of the notes repo should be dynamic, consider it the entire working memory of the user

note workspaces should be organized in a standard way. Most of the contents should be human readable to some extent to make sure users can see the raw information. workspaces will be tracked in git, so perfect archiving of old ideas is not critical, but contraditions and updates that replace old information should be noted and remembered what the old value was so we can ask things like "when did I change X to Y?"

workspaces may be used for anything. IN theory I could take all my life notes in one repo, or I could separate them into personal and work, or separate them by classes I'm taking.

any tests in the workspace should test final behaviro and be long-living. there should not be any tests in the repo that are testing for things that were part of a refactor or something. We should not have any sort of untit test that verifies that I have moved a file from path A to path B. once its moved that's enough of a verification. We only need tests for persistent code that is committed to the repo and is cirtical tot he behavior of the repo.
language of "current design" or "old design" should not exist, we only have the current and we are moving forward.

any scritps should not require an install. they may need some dependencies, but I don't want to do any sort of pip install or virtual environment to make them work. they should be as portable as possible

we probably need some action to perform to close a thread where the agent does a deep dive and makes sure it distills the current information back out to all of the topics/summaries so we are sure they stay up to date. we need to make sure that a thread session is tracked as open until it is officially "closed" when the agent does the summary updates.

how should the skill/entrypoint be triggered? if we are in a specific note-taking workspace should the user just be instructed to set up their agents.md file to indicate that this skill should always be used. The repository should probably include instructions for installing a skill in a local repository instead of for the agent in general. I'm imagining that users will not want a note-taking skill with the directives like this generally installed across their repo.

If we decide to do skills, how should we break them up so that they are useful workflows and also don't all need to be loaded every single time. At a minimum, I'm imagining we have a note-taking skill that outlines the general note-taking process. We have a query skill that tells the agent how to query through the notes, and we have a summary skill that tells the agent how to summarize notes. And potentially we should also have a skill, which maybe is the summary skillormation should be distilled out to the other topics. I'm not sure what other skills should be useful, but I do think it's absolutely critical that the skills do not overlap each other and when it's important they can point to each other so that one skill flows into the other or a skill gets used as part of another skill. I'm imagining something along the lines of when a user makes a query that they can directly use the query skill, but during the note-taking workflow, the agent will understand that it should take the notes, should apply them to the thread, and then it should potentially use the query workflow to answer any on it open questions about the information so far on the thread.

Potentially we also need a skill that explains the hierarchy of the file directories and how to file information though I'm not sure.

I want to make sure that bulk information can be added to the notes database. I think the bulk information would be the equivalent of a single thread, though maybe it has a different storage location, but the information should probably still be distilled out into the summary topics.

I need significant help thinking about how the agent should group topics and distill them down and how this hierarchy should be structural so that future agents can correctly query and work their way through the database and know the hierarchy.

I think it's important that the information be human readable if the user chooses. Documents such as summaries should probably have an agent in mind first with query and information look up being the primary choice, but overall organization helps everybody. So we should make sure that there's a clear way to organize things.

## implementation
In general, I like the idea that the user types additions to the thread in the chat and the agent uses a Python tool to add these snippets to a thread document so that all of these snippets are captured exactly as the user provided them along with some extra metadata for each snippet. Using a Python script to do the append to the thread helps guarantee that these metadata fields and overall structure of the thread are maintained properly.

Next, I think it's also important that after every turn, the agent properly bubbles up the summary and make sure that it propagates into the thread summary and summary doc

track which thread information came from with some sort of tag that can be qeuried later


what is the imposed file structure for making sure notes are organized?
document where rolling thread summary should be kept. independent document or at the top of the thread document?
all file thread files are intended to be organized by date, meaning file path should be a YYYY-MM-DD-<name> format for file name
define task storage format
will there ever be a deeper bubbling of a thread such as during the ending of the thread, where a more vetted search of all topics is performed to make sure the thread has been properly distilled out to the other topics?
Do we require any sort of frontmatter in the topic files to allow agents to quickly understand what they are about without fully scanning them?
How do we trigger the reconcilliation skill?
how does the distillation skill get triggered when it needs to finish resuming an open thread? how does it know if threads have never been distilled? Is there some sort of flag, frontmatter, or metadata in threads that have been updated but not distilled/closed?
CAn we specify the file format (both file type and internal content layout/format) for all of the different document types we expect the agent to work with? Which content is required for each document type? What is the expected file extension? I'm guessing markdown, but how do we make sure the information is in a reliable format?
do the skill specs document how scripts can be shared across multiple skills? do we need a symlink or something? This is less portable than I want it to be, but could be a stopgap
Should we have a better explanation of what "entities" are? Its clear to me what topcis and time would be, but I'm not sure what an entity would be.
please specify that the implementation spec should provide enough detail that I will be able to understand the intent of the contents of every file it plans to create, as well as any additional details you previously planned to include. I don't want to question what will be implemented in there later. I don't however, expect to have full file contents documented in the implementatino plan unless there is no other good way to document it, or if the file is going to be very small. I'm sure there will be exceptions to this.


have one entrypoint skill. tell the agent that this skill should be used every time. serves as the entrypoint for the note taking so that the other skills can trigger automatically. this may just be the general note taking skill
do we need to add guidance for how to choose an open thread or when the agent should decide to open a new one? Does it look 4 threads that are from today and of the current topic or does it just look if there's an active open thread and decide if it should be appending to that? Is it allowed to reopen closed threads?
Can you please provide the query and note skills some more information about what the query process should look like or progressive disclosure information, so the agent understands how to query notes in an efficient way without filling its context? Shouldn't it clearly identify that the index/views should be used to find certain topics? Maybe I'm incorrect. I'm just trying to understand the goal of the index/views and the correct way to search through the repository using progressive disclosure.
ngesting documents, is there... do we need to provide more instructions for how to ingest documents in a way that doesn't fill the context? I'm imagining having a hundred page document and attempting to ingest it. I'm also thinking that every time a document is ingested, it should probably immediately run the full distillation pipeline to make sure that the document is completely ingested. I'm not imagining that a user will append to a document later versus just adding a new document which will retrigger the pipeline/skill.
Right now the responsibilities that you have laid out for the scripts seem to indicate some of the things that I would think only a Agent would be capable of doing. I was imagining that the scripts only manage the structured portions of the work, but that the agent would be required to do things like updating summaries since this is a natural language task. The capture note Python script seems to describe these extra responsibilities or capabilities instead of leaving that to the agent. How do you plan to handle this? Should we instead make sure that the agent knows that it should be handling these tasks and clarify the Python scripts? Goals of just generating new note shells with the specified parameters as Well as appending the raw text to a note? I'm also imagining that the Python script will be extremely useful for time stamping the raw text and adding other metadata, but I don't imagine it will be good at any of the natural language tasks described. I'm also imagining it would be the responsibility of the Python script to apply the timestamp, not the responsibility of the agent.
I'm also very curious how the query will work. I would expect this to very much so be a natural language to ask, completely handled by the agent. I was a Python script going to read unstructured text and identify critical information and linking between concepts so that it can properly answer a user's query that may not be directly answerable with the artifacts at hand. I could, however, understand using a Python script for querying things like notes within a date range or finding thread context related to a stamp in a specific summary. It would be up to the agent though to identify the critical information in the summaries So that the Python script can be provided with the information that it needs to find the backing information for that summary. Maybe this was already your intent, but I don't think it is very clear based on the description of the script that this was the intent. I'm also not so sure how some of the arguments provided to the script would be used if that was the case.
To some extent this is also true for the list action items script. I think it totally makes sense to have fields like filtering by date, but I'm imagining that sometimes it's not going to be clear to the agent that a specific topic will have a certain structured text format that the Python script will be able to use. This could mean that we end up with a sparser than desired query. To some extent, I'd prefer that the queries be a little bit more coarse and then further ingested by the agent to provide more understanding and filtering. I make this entire note-taking skillset l to leverage the natural language processing ability of the agent and just leverage the Python tooling for maintaining structure within the repo and leveraging the structure when necessary to simplify queries. The Python scripts should not create limitations though. They should make the agent more efficient. Some of the arguments provided would be very useful, but I'm afraid that in actual use they may just create limitations.
I also don't think that distilling a thread will necessarily be the task of the Python script. I do think a python script would be a great tool for closing a thread because it needs to do some structured things to maintain the hierarchy, such as marking it closed and moving the file. What I don't understand is how a structured Python script would do tasks like distilling the information out into the other summaries and views to preserve the information. This to me seems like the utmost importance that the agent handles it using natural language. If you have a strong plan, I'm open to it, but I would be extremely cautious to have this completely handled in a Python script.
Additionally, I'm not sure what the resume pending actually does. I would imagine that rerunning distillation on a pending note would have the same effect as resuming a pending distillation. Maybe I'm misguided here and don't fully understand the process, but this script seems redundant, and once again attempting to handle some of the natural language tasks that agents excel at.
I think the igest document script may be extremely useful, but we may have to modify its behavior. I think a primary purpose of the ingest document could be to convert it into a markdown file or some other text file, especially if the input document is a PDF or a .docx or .doc file from Microsoft. Maybe even excel files would be useful eventually. We should probably also investigate further which other formats may be useful as input and figure out how to convert them into text files. Maybe it would be better just to restrict the user to inputting certain documents that are easily convertible within each other. But I think a few of the standard documents for written text could easily be converted using Python to mark down or similar easily ingestible formats. I would Imagine the script would also take the import and the artifact file path and move it into the correct location. I don't however understand how the lightweight extraction would work. Once again, this seems like an inherently natural language task that should be handled completely by the agent.
Does the readme really need to track pending recovery if we already have files sorted by open slash closed? I'm open to your input, but this is just something that stuck out to me. I feel similarly about the active work. I'm surprised that the agent would need to consistently update the readme for these things. And I worry that it will forget to do so or become outdated. maybe this isn't a strong concern though, I'm open to your thoughts. It also seems that this information is a duplicate compared to what is stored in the views. my understanding of the views is that they already track this kind of information.
As I'm reading about how the thread files are set up, I do think it makes a lot of sense that the front matter be updated by a script as well as some of the thread-based metrics with each snippet, such as "distill status" I would want these to continue to be updated by a Python script, but I do believe the summary probably needs some agent natural language inputs instead of depending wholly on a Python script doing these updates. Of course, if you have some scheme where the agent already has determined what the summary update should be, and the script is just being used to track metadata during this update, I would be okay with something like that as well.
one consideration in the threads is I'm not sure that I will do a good job of tracking the speaker in notes I don't have a problem with leaving it in there for when it's useful but Please make sure it's not a explicit requirement
For document imports, I am concerned that the summaries will be too detailed. I think it's important to store a raw text or markdown version of these input files so that an agent can go and search for more information inside of them. The summary should be useful so that the agent understands what is contained in the document and where, so that the agent can go and search within the document for the original source information. I suspect we may need either to never store the actual original document and only store the text conversion of it, which I think is acceptable, especially considering it will become a Git repository and some of these documents do not lend themselves well to Git repository conversion. Though, it could be useful when these documents contain things like images because the user may prefer to go and find the reference inside of the doc themselves. I think I'm convinced to keep both, but I do think having a raw conversion document representation would be very useful for the agent. or maybe the agent should be directed when searching through these docs that it used a tool to convert the doc to raw text every time so that we don't need to store duplicate information. I think that is probably the best scenario, but there should be a clear direction to the agent that manipulation of the file should be done using a text converted format and explain which Tool can do this automatic text conversion for it.
, I'm not sure that we should hold the strong restriction that the type always has to be One of the predetermined lists. I think this list should serve as a starting point and I think topics that are filed in other could eventually be moved out into their own topic if the agent identifies that there is some commonality in the group between these other items. I would imagine this would happen during the deep distillation step. I just worry that there will be items that don't fit inside of the category that consistently end up in other. I also want to protect from the idea that the agent will just consistently make new topics. So I think it's important that we first wait for a collection of items that could make up a new topic before creating that topic. I think it would also be very useful if the user is in the loop when these decisions happen. So creating a new topic should probably require some user approval or be open to user suggestions for what the new topic should be in case they have a better idea.
I think it may make more sense if the views have some more detail about the expectation of their contents and their use. right now it just says which views will exist but I don't think it will be clear to an agent with no context what belongs in these views and how to update them. I think it's also absolutely critical that any agent expected to update them is properly detailed in their skill when they should be updated.
During the thread lifecycle, is it possible for the user to request a deep distillation without actually closing the thread? Does this need to be made an explicit possibility in the skill to make sure that agents understand this?
In the lightweight and deep distillation sections, should it be clear how and when the agent should use any Python tools for this process? In particular, should it be clearly defined to the agent how the agent can generate the appropriate time stamps using a tool? Please also be clear that it is possible to do deep distillation without closing a thread if the user asks for it. Deep distillation can result in new open questins for the user if contradictions or other confusion is found during the distillation. The user may need to provide updates in order to allow the distillation to complete
should We have additional indicators of Pending state based on threads being left open by being left in the open directory or threads that are open and time stamped from a previous day. How do we make sure that two open Agent sessions don't conflict with each other during this recovery process. Should we make it explicit and prompt the user for which threads should be closed once detected? I'd like this kind of edge case clearly documented and indicated in the doc
In the recovery behavior section, I'm also curious how the recoverpending.py file will apply some of the distillation techniques. Once again, this seems like something that the agent should be doing themselves as part of a skill-based workflow, not a Python script. the agent doing the distillation can still leverage the Python scripts as we've discussed earlier.
In the retrieval architecture section, I'm curious why the agent wouldn't start at the index or the read me, considering these things provide overview of what is contained within the repository. It may make sense if everything is sufficiently distilled to the high level that an agent always start out at the highest level and start searching based on what it finds inside of each index or summary document. I don't think a well thought out retrieval process has been laid out in this document this time. I do think this is a first class feature that will be required to be taught to an agent via skill and the agent will not just implicitly know how to handle things. It would be best if the agent be provided with information about best practices and the contents of all of these directories so that it can properly search through everything instead of just being told at the highest level What is expected to be contained inside of its retrieval.
The topic creation rules lower down in the document do make a lot of sense, but topic creation in general seems to contradict the rules provided above that we will have explicit topics. Please make sure that the explicit topic language is updated as we've already discussed.
In the generated views section, should it be made clear that the topic indexes should exist as one index per topic? I don't think the file hierarchy description was very clear about how this should be stored to give the agent information about this separation


### Unasked questions/TODO's

in general, find tasks that should leverage subagents
queries should use subagents to ensure the main agent doesn't fill context
deep ingestion should use a subagent to ensure the main agent doesn't fill context
should we add some sort of description/title to the frontmatter so the agents can leverage it for progressive disclosure during query? Allows agent to know what kinds of things will be in the file without needing to read the entire summary. Should this replace the README.md since it can be concise? do we need a python tool just for reading frontmatter fields efficiently?

# Future TODO:
add python tooling for frontmatter query. Make sure all skills know when to use it
